const Future = @import("task.zig").Future;
const Task = @import("task.zig").Task;
const Channel = @import("channel.zig").Channel;

test "main test" {
    _ = @import("task.zig");
    _ = @import("channel.zig");
}
